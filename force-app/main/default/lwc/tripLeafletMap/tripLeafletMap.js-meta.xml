<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>62.0</apiVersion>
    <isExposed>true</isExposed>
    <masterLabel>Trip Leaflet Map</masterLabel>
    <description>
        Interactive Leaflet.js map for Trip records. Shows departure and
        destination markers (color-coded by location type), a directional
        polyline with arrowhead, and distance label.
    </description>

    <targets>
        <target>lightning__RecordPage</target>
    </targets>

    <targetConfigs>
        <targetConfig targets="lightning__RecordPage">
            <objects>
                <object>Trip__c</object>
            </objects>

            <property name="mapHeight"
                      type="Integer"
                      default="400"
                      label="Map height (px)"
                      description="Height of the map container in pixels."
                      min="200" max="800" />

            <property name="hideRoutePanel"
                      type="Boolean"
                      default="false"
                      label="Hide route panel"
                      description="When checked, hides the route summary panel below the map." />
        </targetConfig>
    </targetConfigs>
</LightningComponentBundle>
