/**
 * Test data factory utility for creating test records in unit tests.
 * Provides static factory methods to generate Salesforce records with consistent test data.
 */
@IsTest
public class TestDataFactory {
    private static Integer sequence = 0;

    private static String nextSuffix() {
        sequence++;
        return String.valueOf(DateTime.now().getTime()) + String.valueOf(sequence);
    }


    /**
     * Creates a test User record.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return User record with test data
     */
    public static User createUser(Boolean doInsert) {
        String suffix = nextSuffix();
        User recordItem = new User(
            Alias = 'tuser',
            Email = 'testuser+' + suffix + '@example.com',
            EmailEncodingKey = 'UTF-8',
            LastName = 'User',
            LanguageLocaleKey = 'en_US',
            LocaleSidKey = 'en_US',
            ProfileId = UserInfo.getProfileId(),
            TimeZoneSidKey = 'America/New_York',
            Username = 'testuser+' + suffix + '@example.com'
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * Creates a test Account record.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return Account record with test data
     */
    public static Account createAccount(Boolean doInsert) {
        Account recordItem = new Account(
            Name = 'Test Account ' + nextSuffix(),
            Description = 'Generated by TestDataFactory'
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test Product2 record.
     * @param doInsert Whether to insert the record into the database.
     * @return The created Product2 record.
     */
    public static Product2 createProduct2(Boolean doInsert) {

        Product2 recordItem = new Product2(
            Name = 'Test Product ' + nextSuffix(),
            IsActive = true,
            Description = 'Generated by TestDataFactory',
            Unit_Weight_Kg__c = 1
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test Bundle_Feature__c record.
     * @param doInsert Whether to insert the record into the database.
     * @return The created Bundle_Feature__c record.
     */
    public static Bundle_Feature__c createBundleFeature(Boolean doInsert) {
        Product2 product = createProduct2(true);
        Bundle_Feature__c recordItem = new Bundle_Feature__c(
            Name = 'Test Feature ' + nextSuffix(),
            Sort_Order__c = sequence,
            Product__c = product.Id,
            Help_Text__c = 'Generated by TestDataFactory',
            Min_Options__c = 0,
            Max_Options__c = 5
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test Bundle_Option__c record.
     * @param doInsert Whether to insert the record into the database.
     * @return The created Bundle_Option__c record.
     */
    public static Bundle_Option__c createBundleOption(Boolean doInsert) {
        Product2 bundleProduct = createProduct2(true);
        Product2 optionProduct = createProduct2(true);
        Bundle_Feature__c feature = createBundleFeature(true);
        
        Bundle_Option__c recordItem = new Bundle_Option__c(
            Option_Type__c = 'Component',
            Sort_Order__c = sequence,
            Bundle_Product__c = bundleProduct.Id,
            Option_Product__c = optionProduct.Id,
            Feature__c = feature.Id,
            Default_Quantity__c = 1,
            Min_Quantity__c = 0,
            Max_Quantity__c = 10,
            Is_Selected__c = false,
            Is_Required__c = false,
            Quantity_Editable__c = false
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * Creates a test Opportunity record with default OfferType.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return Opportunity record with test data
     */
    public static Opportunity createOpportunity(Boolean doInsert) {
        Account accountRecord = createAccount(true);
        Opportunity recordItem = new Opportunity(
            Name = 'Test Opportunity ' + nextSuffix(),
            AccountId = accountRecord.Id,
            StageName = 'Prospecting',
            CloseDate = Date.today().addDays(30),
            Offer_Type__c = 'Sale',
            Description = 'Generated by TestDataFactory',
            Is_Transport_Required__c = false
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }


    /**
     * Creates a test Opportunity record with specified OfferType.
     * 
     * @param offerType The Offer_Type__c field value
     * @param doInsert If true, inserts the record into the database
     * @return Opportunity record with test data
     */
    public static Opportunity createOpportunity(String offerType, Boolean doInsert) {
        Account accountRecord = createAccount(true);
        Opportunity recordItem = new Opportunity(
            Name = 'Test Opportunity ' + nextSuffix(),
            AccountId = accountRecord.Id,
            StageName = 'Prospecting',
            CloseDate = Date.today().addDays(30),
            Offer_Type__c = offerType,
            Description = 'Generated by TestDataFactory',
            Is_Transport_Required__c = false
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }


    /**
     * Creates a test ProductCatalog record with auto-generated name.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return ProductCatalog record with test data
     */
    public static ProductCatalog createProductCatalog(Boolean doInsert) {
        ProductCatalog recordItem = new ProductCatalog(
            Name = 'Test Catalog ' + nextSuffix()
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * Creates a test ProductCatalog record with specified name.
     * 
     * @param catalogName The catalog name
     * @param doInsert If true, inserts the record into the database
     * @return ProductCatalog record with test data
     */
    public static ProductCatalog createProductCatalog(String catalogName, Boolean doInsert) {
        ProductCatalog recordItem = new ProductCatalog(
            Name = catalogName
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }


    /**
     * Creates a test ProductCategory record with default catalog.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return ProductCategory record with test data
     */
    public static ProductCategory createProductCategory(Boolean doInsert) {
        ProductCatalog catalogRecord = createProductCatalog(true);
        ProductCategory recordItem = new ProductCategory(
            Name = 'Test Category ' + nextSuffix(),
            CatalogId = catalogRecord.Id,
            IsNavigational = true,
            SortOrder = 1
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * Creates a test ProductCategory record with specified parameters.
     * 
     * @param catalogId The parent ProductCatalog ID
     * @param parentCategoryId The parent ProductCategory ID (optional)
     * @param categoryName The category name
     * @param isNavigational The IsNavigational flag
     * @param doInsert If true, inserts the record into the database
     * @return ProductCategory record with test data
     */
    public static ProductCategory createProductCategory(
        Id catalogId,
        Id parentCategoryId,
        String categoryName,
        Boolean isNavigational,
        Boolean doInsert
    ) {
        ProductCategory recordItem = new ProductCategory(
            Name = categoryName,
            CatalogId = catalogId,
            ParentCategoryId = parentCategoryId,
            IsNavigational = isNavigational,
            SortOrder = 1
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }


    /**
     * Creates a test ProductCategoryProduct record with auto-generated category and product.
     * 
     * @param doInsert If true, inserts the record into the database
     * @return ProductCategoryProduct record with test data
     */
    public static ProductCategoryProduct createProductCategoryProduct(Boolean doInsert) {
        ProductCategory categoryRecord = createProductCategory(true);
        Product2 productRecord = createProduct2(true);
        ProductCategoryProduct recordItem = new ProductCategoryProduct(
            ProductCategoryId = categoryRecord.Id,
            ProductId = productRecord.Id
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * Creates a test ProductCategoryProduct record with specified category and product.
     * 
     * @param categoryId The ProductCategory ID
     * @param productId The Product2 ID
     * @param doInsert If true, inserts the record into the database
     * @return ProductCategoryProduct record with test data
     */
    public static ProductCategoryProduct createProductCategoryProduct(
        Id categoryId,
        Id productId,
        Boolean doInsert
    ) {
        ProductCategoryProduct recordItem = new ProductCategoryProduct(
            ProductCategoryId = categoryId,
            ProductId = productId
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test Pricebook2 record.
     * @param name The pricebook name.
     * @param isActive Whether the pricebook is active.
     * @param doInsert Whether to insert the record into the database.
     * @return The created Pricebook2 record.
     */
    public static Pricebook2 createPricebook2(String name, Boolean isActive, Boolean doInsert) {
        Pricebook2 recordItem = new Pricebook2(
            Name = name,
            IsActive = isActive,
            Description = 'Generated by TestDataFactory'
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test PricebookEntry record.
     * @param pricebookId The Pricebook2 ID.
     * @param product2Id The Product2 ID.
     * @param unitPrice The unit price for the entry.
     * @param isActive Whether the entry is active.
     * @param doInsert Whether to insert the record into the database.
     * @return The created PricebookEntry record.
     */
    public static PricebookEntry createPricebookEntry(Id pricebookId, Id product2Id, Decimal unitPrice, Boolean isActive, Boolean doInsert) {
        PricebookEntry recordItem = new PricebookEntry(
            Pricebook2Id = pricebookId,
            Product2Id = product2Id,
            UnitPrice = unitPrice,
            IsActive = isActive,
            UseStandardPrice = false
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }

    /**
     * @description Creates a test Opportunity record with specified OfferType and Pricebook.
     * @param offerType The Offer_Type__c field value.
     * @param pricebookId The Pricebook2Id for the opportunity.
     * @param doInsert If true, inserts the record into the database.
     * @return Opportunity record with test data.
     */
    public static Opportunity createOpportunityWithPricebook(String offerType, Id pricebookId, Boolean doInsert) {
        Account accountRecord = createAccount(true);
        Opportunity recordItem = new Opportunity(
            Name = 'Test Opportunity ' + nextSuffix(),
            AccountId = accountRecord.Id,
            StageName = 'Prospecting',
            CloseDate = Date.today().addDays(30),
            Offer_Type__c = offerType,
            Pricebook2Id = pricebookId,
            Description = 'Generated by TestDataFactory',
            Is_Transport_Required__c = false
        );
        if (doInsert) {
            insert recordItem;
        }
        return recordItem;
    }


    /*
    public static Bundle_Feature__c createBundleFeature(Boolean doInsert) {
        Product2 productRecord = createProduct2(true);
        Bundle_Feature__c recordItem = new Bundle_Feature__c(
            Name = 'Test Feature ' + nextSuffix(),
            Product__c = productRecord.Id,
            Sort_Order__c = 10,
            Help_Text__c = 'Generated by TestDataFactory'
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Bundle_Option__c createBundleOption(Boolean doInsert) {
        Product2 bundleProduct = createProduct2(true);
        Product2 optionProduct = createProduct2(true);
        Bundle_Feature__c featureRecord = createBundleFeature(true);
        Bundle_Option__c recordItem = new Bundle_Option__c(
            Bundle_Product__c = bundleProduct.Id,
            Option_Product__c = optionProduct.Id,
            Feature__c = featureRecord.Id,
            Option_Type__c = 'Component',
            Sort_Order__c = 10,
            Default_Quantity__c = 1,
            Min_Quantity__c = 1,
            Max_Quantity__c = 5,
            Quantity_Editable__c = true,
            Is_Required__c = false,
            Is_Selected__c = false
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Bundle_Rule_Assignment__c createBundleRuleAssignment(Boolean doInsert) {
        Product2 bundleProduct = createProduct2(true);
        Product_Rule__c productRuleRecord = createProductRule(true);
        Bundle_Rule_Assignment__c recordItem = new Bundle_Rule_Assignment__c(
            Bundle_Product__c = bundleProduct.Id,
            Product_Rule__c = productRuleRecord.Id,
            Is_Active__c = true
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Location createLocation(Boolean doInsert) {
        Location recordItem = new Location(
            Name = 'Test Location ' + nextSuffix(),
            Country_Code__c = 'US',
            Description = 'Generated by TestDataFactory'
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static OpportunityLineItem createOpportunityLineItem(Boolean doInsert) {
        Opportunity opportunityRecord = createOpportunity(true);
        Id standardPricebookId = Test.getStandardPricebookId();
        Product2 productRecord = createProduct2(true);
        PricebookEntry pricebookEntryRecord = createStandardPricebookEntry(productRecord.Id, standardPricebookId);
        opportunityRecord.Pricebook2Id = standardPricebookId;
        update opportunityRecord;
        OpportunityLineItem recordItem = new OpportunityLineItem(
            OpportunityId = opportunityRecord.Id,
            PricebookEntryId = pricebookEntryRecord.Id,
            Quantity = 1,
            UnitPrice = 100,
            ServiceDate = Date.today().addDays(1),
            Description = 'Generated by TestDataFactory',
            Delivery_Date__c = Date.today().addDays(2),
            Rental_Start_Date__c = Date.today().addDays(2),
            Rental_End_Date__c = Date.today().addDays(5),
            Pickup_Date__c = Date.today().addDays(5)
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Product_Rule__c createProductRule(Boolean doInsert) {
        Product_Rule__c recordItem = new Product_Rule__c(
            Name = 'Test Rule ' + nextSuffix(),
            Conditions_Met__c = 'All',
            Evaluation_Event__c = 'Save',
            Scope__c = 'Bundle',
            Type__c = 'Validation',
            Is_Active__c = true,
            Message__c = 'Generated by TestDataFactory'
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Rule_Action__c createRuleAction(Boolean doInsert) {
        Product_Rule__c productRuleRecord = createProductRule(true);
        Product2 productRecord = createProduct2(true);
        Rule_Action__c recordItem = new Rule_Action__c(
            Product_Rule__c = productRuleRecord.Id,
            Type__c = 'Add',
            Target_Product__c = productRecord.Id,
            Sort_Order__c = 10
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Rule_Condition__c createRuleCondition(Boolean doInsert) {
        Product_Rule__c productRuleRecord = createProductRule(true);
        Rule_Condition__c recordItem = new Rule_Condition__c(
            Product_Rule__c = productRuleRecord.Id,
            Filter_Type__c = 'Value',
            Filter_Value__c = 'Prospecting',
            Operator__c = 'equals',
            Tested_Field__c = 'StageName',
            Tested_Object__c = 'Opportunity',
            Index__c = 1
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Trip__c createTrip(Boolean doInsert) {
        Opportunity opportunityRecord = createOpportunity(true);
        Location departureLocation = createLocation(true);
        Location destinationLocation = createLocation(true);
        Trip__c recordItem = new Trip__c(
            Opportunity__c = opportunityRecord.Id,
            Country_Code__c = 'US',
            Direction__c = 'Delivery',
            Status__c = 'Draft',
            Trip_Date__c = Date.today().addDays(3),
            Trip_Key__c = 'TRIP-' + nextSuffix(),
            Departure_Location__c = departureLocation.Id,
            Destination_Location__c = destinationLocation.Id,
            System_Price__c = 100,
            Final_Price__c = 100,
            Total_Weight_Kg__c = 50
        );
        if (recordItem.Departure_Location__c == recordItem.Destination_Location__c) {
            recordItem.Destination_Location__c = createLocation(true).Id;
        }
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Volume_Pricing_Schedule__c createVolumePricingSchedule(Boolean doInsert) {
        Product2 productRecord = createProduct2(true);
        Volume_Pricing_Schedule__c recordItem = new Volume_Pricing_Schedule__c(
            Name = 'Test Volume Schedule ' + nextSuffix(),
            Product__c = productRecord.Id,
            Pricebook__c = Test.getStandardPricebookId(),
            Pricing_Type__c = 'Tiered',
            Overage_Rate__c = 10,
            Is_Active__c = true
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    public static Volume_Pricing_Tier__c createVolumePricingTier(Boolean doInsert) {
        Volume_Pricing_Schedule__c scheduleRecord = createVolumePricingSchedule(true);
        Volume_Pricing_Tier__c recordItem = new Volume_Pricing_Tier__c(
            Name = 'Tier ' + nextSuffix(),
            Volume_Pricing_Schedule__c = scheduleRecord.Id,
            Lower_Bound__c = 1,
            Upper_Bound__c = 10,
            Price__c = 100,
            Sort_Order__c = 10
        );
        if (doInsert) { insert recordItem; }
        return recordItem;
    }

    private static PricebookEntry createStandardPricebookEntry(Id productId, Id pricebookId) {
        PricebookEntry pricebookEntryRecord = new PricebookEntry(
            Pricebook2Id = pricebookId,
            Product2Id = productId,
            UnitPrice = 100,
            IsActive = true,
            UseStandardPrice = false
        );
        insert pricebookEntryRecord;
        return pricebookEntryRecord;
    }
    */
}