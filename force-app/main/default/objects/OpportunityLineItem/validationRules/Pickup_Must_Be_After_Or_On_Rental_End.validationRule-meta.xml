<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Pickup_Must_Be_After_Or_On_Rental_End</fullName>
    <active>true</active>
    <description>Enforces that Pickup_Date__c cannot precede Rental_End_Date__c, ensuring equipment retrieval is only scheduled after the rental period has fully elapsed.</description>
    <errorConditionFormula>AND(NOT(ISBLANK(Rental_End_Date__c)), NOT(ISBLANK(Pickup_Date__c)), Pickup_Date__c &lt; Rental_End_Date__c)</errorConditionFormula>
    <errorDisplayField>Pickup_Date__c</errorDisplayField>
    <errorMessage>Pickup Date cannot be before the Rental End Date. The equipment must be picked up after the rental period ends.</errorMessage>
</ValidationRule>
